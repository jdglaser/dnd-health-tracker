services:
  # web:
  #   build: .
  #   ports:
  #     - "8080:8080"
  #   depends_on:
  #     db:
  #       condition: service_healthy
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: postgres
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "foo"]
      interval: 1s
      timeout: 5s
      retries: 10
    ports:
      - 5432:5432